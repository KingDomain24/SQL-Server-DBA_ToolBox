--TSQL Script to find out SQL Agent Job details (Name, Status and Owner)

USE msdb
go
SELECT j.[name] AS 'JobName',
Enabled = CASE
WHEN j.enabled = 0 THEN 'No'
ELSE 'Yes'
END,
l.[name] AS 'OwnerName'
FROM   msdb.dbo.sysjobs j
INNER JOIN master.dbo.syslogins l
ON j.owner_sid = l.sid
ORDER  BY j.[name]
 

--TSQL Script to generate job owner update commands for all SQL Agent Jobs to sa

SET nocount ON
SELECT 'EXEC MSDB.dbo.sp_update_job ' + Char(13)
+ '@job_name = ' + Char(39) + j.[name] + Char(39)
+ ',' + Char(13) + '@owner_login_name = '
+ Char(39) + 'sa' + Char(39) + Char(13) + Char(13)
FROM   msdb.dbo.sysjobs j
INNER JOIN master.dbo.syslogins l
ON j.owner_sid = l.sid
WHERE  l.[name] <> 'sa'
ORDER  BY j.[name]

Reference: 
https://exploresqlserver.wordpress.com/2016/11/15/script-to-update-owner-for-all-sql-agent-jobs-to-sa/
